# Implementation Plans

Generated by the improve skill on 2026-06-23. Base commit: `672167a`.

Execute in the order below unless dependencies say otherwise. Each executor: read the plan fully before starting, honor its STOP conditions, and update your row when done.

## Execution order & status

| Plan | Title | Priority | Effort | Depends on | Status |
|------|-------|----------|--------|------------|--------|
| 001 | PaperTrader error-handling hardening | P1 | S | — | TODO |
| 002 | DDP correctness — barrier, scaler broadcast, CUDA guard | P1 | M | — | TODO |
| 003 | Document msrr_loss is a heuristic, not Sharpe optimization | P2 | S | — | TODO |
| 004 | Walk-forward fold isolation | P1 | M | — | TODO |
| 005 | Redact API keys from colab template | P2 | S | — | TODO |
| 006 | Threshold optimization — Platt scaling | P2 | M | — | TODO |
| 007 | Cache raw_data across trade cycles | P2 | S | — | TODO |
| 008 | Add retry to yfinance download | P2 | S | — | TODO |
| 009 | Add CI pipeline | P1 | M | — | TODO |
| 010 | Add tests for untested files | P2 | L | 001, 003, 005 | TODO |
| 011 | Add missing type annotations | P3 | L | 010 | TODO |
| 012 | Minor bug fixes (NaN clip, RSI, gradient accum) | P3 | S | — | TODO |

## Dependency notes

- **010** requires **001** (paper_trader fixes) and **003** (msrr rename) and **005** (secrets redact) because test assertions reference the fixed/new names.
- **011** requires **010** (add annotations after tests are in place to avoid conflicts).

No other ordering constraints — plans 001-009 and 012 are independent.

## Findings considered and rejected

- **F6 (market holiday awareness)**: Not worth the complexity for a fallback that already works correctly 99% of the time.
- **F9 (unknown position side)**: Alpaca returns `"long"`/`"short"` reliably; adding defensive code for an improbable edge case creates dead code.
- **F13 (mtime-based cache hash)**: Conservative behavior (false cache miss > false cache hit) is correct for ML.
- **F19 (pretrain loss factor)**: Constant factor absorbed by learning rate — not a bug.
- **F20 (hardcoded seed 42)**: Single pretrain runs aren't affected; ablation studies are not the primary use case.
- **F21 (equal-weight portfolio)**: Internally consistent for threshold comparison — changing to long-short doesn't improve the optimization.
- **F23 (n_samples_seen_=1)**: `inverse_transform` is not used anywhere; guard against future misuse is premature.
- **F24 (tempfile leak)**: Single tempfile on a workstation, not an issue.
- **F25 (walk-forward zero folds)**: Handled by the existing size validation from earlier code review fixes.
- **F26 (ticker cache)**: Adds complexity for 2s startup saving — not worth it.
- **F29 (CSV metadata)**: `auto_adjust=True` is hardcoded and unlikely to change.
- **F30 (exec globals)**: The exec'd code is project code; Colab secrets exposure is a documentation issue.
- **F3 (walk-forward rebuild)**: Rendered moot by plan 004's fold file detection.
- **F4 (dead guard)**: Fixed as part of plan 004.
- **F27 (int display)**: Functionally correct — not a bug.
