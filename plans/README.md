# Implementation Plans

Generated by the improve skill on 2026-06-28. Execute in the order below unless
dependencies say otherwise.

## Execution order & status

### Active plans (this run)

| Plan | Title | Priority | Effort | Depends on | Status |
|------|-------|----------|--------|------------|--------|
| 001 | Fix .env loading — quotes stripping + all entry points | P1 | S | — | TODO |
| 002 | Fix docs — architecture claims + hyperparameters + CLI defaults | P1 | S | — | TODO |
| 003 | Add Juneteenth + Good Friday to NYSE holidays | P1 | S | — | TODO |
| 004 | Add test coverage for untested critical paths | P2 | M | — | TODO |
| 005 | Add pre-commit hooks (ruff check + format) | P3 | S | — | TODO |

### Superseded plans (prior run, 2026-06-23)

| Plan | Title | Status |
|------|-------|--------|
| 001-paper-trader-hardening.md | PaperTrader hardening (dual clients, portfolio cap, etc.) | DONE — implemented in `605fef8` |
| 002-ddp-barrier-scaler-cudaguard.md | DDP barrier + scaler + CUDA guard | DONE — implemented in `d87f3b5` |
| 003-msrr-loss-rename.md | MSRR → portfolio_mse_loss rename | DONE — implemented in `402b5d1` |
| 004-walkforward-fold-isolation.md | Walk-forward fold model isolation | DONE — fold_i_test.npz removed |
| 005-secrets-redact-colab-template.md | Secrets redaction in colab template | DONE — implemented in `dea2dae` |
| 006-threshold-isotonic-regression.md | Replace isotonic with Platt scaling | DONE — implemented in earlier session |
| 007-cache-raw-data-across-cycles.md | Raw data cache across inference cycles | DONE — implemented via `_raw_data_cache` |
| 008-yfinance-retry.md | Tenacity retry on yfinance | DONE — implemented via `_retry()` wrapper |
| 009-ci-pipeline.md | CI pipeline setup | DONE — `.github/workflows/ci.yml` exists |
| 010-test-coverage.md | Test coverage (prior run) | SUPERSEDED by 004-add-test-coverage.md |
| 011-type-annotations.md | Type annotations cleanup | DEFERRED — mypy at 17 errors, known noise floor |
| 012-minor-bug-fixes.md | Minor bug fixes | DONE — fixed across `dea2dae`–`d54d624` |

## Dependency notes

- No dependencies between active plans — all can be executed in parallel.
- Plans 001–003 are independent bug/doc fixes.
- Plan 004 is the highest-effort but highest-leverage (test coverage for core pipeline).
- Plan 005 is pure DX polish.

## Findings considered and rejected

- **Dead stock_embed.weight lookup (explore-17 F3)**: False positive — `stock_embed.weight` is used at `src/utils.py:77-78` to infer `n_stocks` from checkpoints for self-describing models. Active, correct code.
- **Order return value ignored in reconcile (explore-15 F2)**: False positive — the code correctly catches exceptions from `submit_market_order` and records `BUY_FAIL`/`SELL_FAIL` tuples. Return values from Alpaca's `submit_order` are not needed.
- **Weak cache fingerprint CRC32 (explore-15 F11)**: Downgraded to LOW. CRC32 on first 4096 bytes is adequate for the append-only use case. Not worth the complexity.
- **Config dataclass holds API keys (explore-15 F10)**: Downgraded to LOW. Keys are never serialized, logged, or persisted. `_redact_secrets` handles the Colab template.
- **market_open/next_open crash on None (explore-15 F3)**: Downgraded to LOW. Alpaca SDK always returns a Clock object, never None.
- **Config mutated in 4 entry points (explore-17 F20)**: Deferred as architectural. Requires a Config factory pattern across 4 files. Low risk.
- **_NoopTqdm global monkey-patch (explore-17 F15)**: Downgraded. Already fixed — scoped to on_mount/unmount.
- **Missing py.typed / CI mypy gate (explore-17 F9, F22)**: Deferred. 17 pre-existing errors from Alpaca SDK type stubs. Adding a gate requires resolving those first.
